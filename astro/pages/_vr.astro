<style>
  @import url("https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&family=Instrument+Sans:ital,wght@0,400..700;1,400..700&family=Instrument+Serif:ital@0;1&family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Source+Serif+4:ital,opsz,wght@0,8..60,200..900;1,8..60,200..900&display=swap");

  :root {
    --foreground-color: #dbdbdb;
    --background-color: #0f0f0f;

    /* Text Configuration */
    --font-size: 1rem;
    --line-height: 1.5; /* Can be changed to 1.8 etc */

    /* The Holy Grid Unit */
    --baseline: calc(var(--font-size) * var(--line-height));

    /* Spacing Multiples */
    --space-1: var(--space);
    --space-2: var(--space-2);
    --space-3: var(--space-3);
    --space-4: var(--space-4);

    /* Scale (Modular scale based on ratio, but must snap to grid) */
    --ratio: var(--line-height);
    --scale-base: var(--font-size);
    --scale-1: calc(var(--scale-base) * var(--ratio));
    --scale-2: calc(var(--scale-base) * var(--ratio) * var(--ratio));
    --scale-3: calc(
      var(--scale-base) * var(--ratio) * var(--ratio) * var(--ratio)
    );
  }

  * {
    padding: 0;
    margin: 0;

    /* Optional: Cap height trimming can help, but let's stick to strict box model first */
    /* text-box-trim: trim-both; */
    /* text-box-edge: cap alphabetic; */
  }

  html,
  body {
    position: relative;
    font-size: var(--font-size);
    line-height: var(--line-height);
    color: var(--foreground-color);
    background-color: var(--background-color);
    font-family: "Instrument Sans", "Inter", sans-serif;
  }

  article {
    max-width: 42rem;
    margin: auto;
    padding-block: var(--space-2);
  }

  /*
   Universal Rhythm Enforcer
   Every block element gets a bottom margin of 1 baseline unit.
   This ensures the NEXT element starts on a baseline.
*/
  article > * {
    margin-block-end: var(--space-1);
  }

  /*
   Headers
   Must have a line-height that is a multiple of the baseline.
   Or, we let the natural line-height be, but ensure the total box height (content + margins)
   is a multiple of the baseline.

   Strategy:
   1. Set font-size.
   2. Calculate required line-height to fit nearest baseline multiple.

   Actually, simpler strategy for "Holy Grail":
   Force line-height to be an integer multiple of the baseline relative to the font-size?
   No, line-height is usually unitless.

   Let's try setting explicit line-heights in 'rem' that match the grid.
*/

  h1 {
    font-size: var(--scale-3);
    /* Calculate nearest baseline multiple for line-height */
    /* If scale-3 is large, we might need 2 or 3 baselines height */
    line-height: var(--space-3);
    margin-block-end: var(--space-2);
  }

  h2 {
    font-size: var(--scale-2);
    line-height: var(--space-2);
    margin-block-end: var(--space-1);
    margin-block-start: var(--space-2);
  }

  h3 {
    font-size: var(--scale-1);
    line-height: var(--space-2);
    margin-block-end: var(--space-1);
    margin-block-start: var(--space-1);
  }

  h4,
  h5,
  h6 {
    font-size: var(--scale-base);
    line-height: var(--space);
    margin-block-end: var(--space-1);
  }

  p {
    margin-block-end: var(--space-1);
  }

  /* List handling */
  ul,
  ol {
    margin-block-end: var(--space-1);
    padding-inline-start: 1.5rem;
    /* Ensure no default top margin breaks the rhythm */
    margin-block-start: var(--space-1);
  }

  /* If a list follows a paragraph, the paragraph's bottom margin + list's top margin
   might create double spacing. We can collapse it or just be explicit.
   Standard CSS margin collapsing should handle it, but let's be safe.
*/

  li {
    /* Ensure each list item takes up a multiple of the baseline */
    /* If text wraps, line-height handles it. */
    /* If we want extra spacing between items: */
    /* margin-block-end: var(--space-1); */
  }

  li + li {
    /* Optional: Add spacing between list items if desired, but must be baseline multiple */
    /* margin-block-start: var(--space-0-5?); No, must be full baseline or 0 */
  }

  /* Grid Overlay */
  html::before {
    --color-grid: #e58f5c;
    --percent-grid: 50%;
    content: "";
    position: absolute;
    inset: 0;
    background-image: repeating-linear-gradient(
      to bottom,
      transparent 0,
      transparent calc(var(--space) - 1px),
      color-mix(in srgb, var(--color-grid) var(--percent-grid), transparent)
        calc(var(--space) - 1px),
      color-mix(in srgb, var(--color-grid) var(--percent-grid), transparent)
        var(--space)
    );
    pointer-events: none;
    z-index: 10000;
    /* Align grid to start of content if needed, but usually top:0 is fine if body has no weird margin */
  }
</style>

<article>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
    Lorem Ipsum has been the industry's standard dummy text ever since the
    1500s, when an unknown printer took a galley of type and scrambled it to
    make a type specimen book.
  </p>
  <p>
    It has survived not only five centuries, but also the leap into electronic
    typesetting, remaining essentially unchanged. It was popularised in the
    1960s with the release of Letraset sheets containing Lorem Ipsum passages,
    and more recently with desktop publishing software like Aldus PageMaker
    including versions of Lorem Ipsum.
  </p>
  <p>It has survived not only five centuries</p>
  <h1>Title</h1>
  <p>It has survived not only five centuries</p>
  <p>It has survived not only five centuries</p>
  <h2>yup it works</h2>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
    Lorem Ipsum has been the industry's standard dummy text ever since the
    1500s, when an unknown printer took a galley of type and scrambled it to
    make a type specimen book. of my lige
  </p>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
    Lorem Ipsum has been the industry's standard dummy text ever since the
    1500s, when an unknown printer took a galley of type and scrambled it to
    make a type specimen book. of my life
  </p>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
    Lorem Ipsum has been the industry's standard dummy text ever since the
    1500s, when an unknown printer took a galley of type and scrambled it to
    make a type specimen book. of my life
  </p>

  <!--p>
    <img src="https://cdn.mos.cms.futurecdn.net/Xm5tNGu2r7gT9WzeXqd8S9-1200-80.jpg.webp" width="70vw" />
  </p-->
  <h3>Header 3</h3>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
  </p>
  <blockquote>Lorem Ipsum is simply dummy text</blockquote>
  <h4>Header 4</h4>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
  </p>
  <p>Lorem Ipsum is simply dummy text</p>
  <h5>Header 5</h5>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
  </p>
  <ul>
    <li>hola all todos</li>
    <li>hola all todos</li>
    <li>hola all todos</li>
  </ul>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
  </p>
  <h6>Header 6</h6>
  <p>
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
    Lorem Ipsum has been the industry's standard dummy text ever since the
    1500s, when an unknown printer took a galley of type and scrambled it to
    make a type specimen book. It has survived not only five centuries, but also
    the leap into electronic typesetting, remaining essentially unchanged. It
    was popularised in the 1960s with the release of Letraset sheets containing
    Lorem Ipsum passages, and more recently with desktop publishing software
    like Aldus PageMaker including versions of Lorem Ipsum.
  </p>
</article>
