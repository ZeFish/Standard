<!DOCTYPE html>
<html lang="{{ site.language }}" class="rhythm" data-theme="{{ theme }}">
    <head>
      {% include standard.layouts.meta %}
    </head>
    {% standardFonts %}
    {% standardAssets %}
    {% standardLab %}
    <link rel="stylesheet" href="/assets/css/style.css">
    <body {% bodyHtmx %}>
		<header class="flex center-content" style="justify-content: space-between; max-width: 100%;">
			<div class="flex" style="align-items: left;">
				<a href="/" style="text-decoration: none;">{{ site.title }}</a>
			</div>
			{% menu site.nav.header, {className: "small margin-0 padding-0"} %}
		</header>
     	<main class="prose md">
        {{ content | safe }}
        {% standardComment %}
      </main>
			<footer class="prose smaller">
			  <hr >
				<p>© {{ now.getFullYear() }} {{ site.author.name }}. <a href="/">{{ site.title }}</a> | <a href="https://github.com/ZeFish/Standard/blob/main/LICENSE" class="external-link">MIT License</a> | {{ pkg.version }}</p>
			</footer>
	</body>
</html>
